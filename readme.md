# C++ Projects Sandbox

My personal workspace for C++ projects and experiments.

## 🛠️ How to Compile & Run

Each project is a standalone directory. Most are single-file — just compile and run:

```bash
cd Project-Name
g++ main.cpp -o main
./main   
